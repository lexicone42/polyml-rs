# gauss_final.sml is the self-contained driver the isabelle_gauss test runs; it
# EMBEDS the whole chain, so the per-stage campaign drivers below are superseded
# scratch — kept locally for resume but not tracked.
gauss_foundation.sml
gauss_appendix.sml
abs_inj.sml
_abs_inj_delta.sml
gauss_lemma.sml
gauss_stageP.sml
gauss_final_appendix.sml
recon_plan.json
qr_foundation_wf.js
# NB: qr_f1_toolbox.sml is NOT superseded scratch — the isabelle_quadratic_
# reciprocity test READS it (the first of its 5 concatenated pieces), so it is
# TRACKED (must be present for a clean-checkout QR reproduction). Only qr_f2.sml
# (the QR test uses qr_f2_appendix.sml instead) is genuinely superseded.
gauss_progress.json
eisenstein_recon.json
qr_f2.sml
