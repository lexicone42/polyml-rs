# _assembled_base.sml IS tracked: tools/build-l4-checkpoint.sh exports the
# /tmp/l4_foursq_star checkpoint from it (it's the live-context base = base.sml +
# the part-A/B/C deltas). Keeping it verbatim avoids reconstruction risk.

# Run logs
*.log
# Redundant alternate stage-AB partial (seat1 + seat3 deltas cover this)
seat2_descent_stageAB_delta.sml
