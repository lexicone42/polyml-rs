//! The `poly` binary entry point.
//!
//! Monday-milestone scope: load a pexport heap image, transfer control
//! to its root function, exit cleanly. Everything else is stubbed.

fn main() {
    eprintln!("polyml-rs: scaffold only; nothing implemented yet.");
    std::process::exit(2);
}
